<resources>

    <style name="Theme.Keeper.Core" parent="Theme.Keeper">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Component.Keeper.BottomSheet" parent="Component.Keeper.BottomSheet.Bridge">
        <item name="android:navigationBarColor">@color/brand_surface</item>
    </style>

    <style name="Component.Keeper.Picker.Core">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/brand_background_content</item>
    </style>

</resources>